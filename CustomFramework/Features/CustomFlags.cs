using PlayerRoles;

namespace CustomFramework.Features
{
    public static class CustomFlags
    {
        public static RoleChangeReason SpecifiedTeam = (RoleChangeReason)100;
        public static RoleChangeReason DontChangeSubclass = (RoleChangeReason)101;
    }
}
